# SQA / PIMC discrete portfolio optimisation

Simulated quantum annealing (path-integral Monte Carlo) for
mean–variance portfolios with integer weights and a budget constraint.

## Setup
```bash
pip install -r requirements.txt
